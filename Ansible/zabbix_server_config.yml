- name: Configure Zabbix Server
  hosts: zabbix_servers
  vars:
    zabbix_version: "7.0"
    db_name: "zabbix"
    db_user: "zabbix"
    db_pass: "zabbix"
    ansible_user: ubuntu
    ansible_ssh_private_key_file: /home/ubuntu/.ssh/id_rsa
  become: yes
  roles:
    - zabbix_server